# 2026-07-11 — Fleet structural-uniformity audit: Clear findings recorded (#285–#288)

A fleet-wide structural-uniformity audit compared the four NeNe Tier A
products (invoice / clear / deal / vault) across NENE2 usage, auth/session,
multi-tenancy, installer, frontend, and the demo machinery. Every
Clear-relevant finding was re-verified against this repository's code before
filing anything (one fleet-report estimate was corrected on the way: the
`?? 0` org-scoping idiom appears in 38 handler sites, not "15+").

Recorded in [`docs/audit/2026-07-11-structural-uniformity.md`](../audit/2026-07-11-structural-uniformity.md):

- **Strengths confirmed** (fleet-leading, keep): best login-throttle spec of
  the four products (email+IP, 5/15 min + lock, hashed identifiers), login
  timing equalization, `login_failed`/`login_succeeded` audit events, the
  only shipped MFA (TOTP) backend, demo-sweep UTC regression tests, and a
  real JWT fail-close (empty secret → health-only surface).
- **High-impact divergences → dedicated issues**: #285 — the JWT layer is
  bespoke (`firebase/php-jwt` direct, `NENE_CLEAR_JWT_SECRET`, shadow
  `TokenIssuerInterface`) instead of the fleet-standard
  `GuardedJwtSecretResolver` + `LocalBearerTokenVerifier`; #286 — NENE2 is
  consumed as a path repo `@dev` (lock ref 2026-07-02, pre-v1.10.0), so
  releases are not reproducible and the existing 0.1.0 zip vendors a NENE2
  without the demo error-page renderer (throttle/capacity errors would render
  as raw JSON).
- **Remaining findings → checklist issue #287**: bespoke TOTP core vs NENE2
  v1.10.0, `ConfigLoader` unused / 221-line `index.php` composition root,
  missing branded demo error page, no openapi/mcp gate in `composer check`,
  `?? 0` org-scope propagation (38 sites) + zero tenant-resolution tests,
  frontend layout/codegen/i18n/E2E divergences, double font loading,
  installer guard-only cleanup.

No code changed today — issues + docs only (#288 is this recording task).
