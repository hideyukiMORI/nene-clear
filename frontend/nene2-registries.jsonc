// nene2 legacy manifest — NeNe Clear frontend (W1 @layer legacy isolation pilot).
//
// MACHINE-GENERATED by `nene2-check init --scan` (@hideyukimori/nene2-standards@1.0.0).
// Regenerate; do NOT hand-edit path/maxLines/maxBytes (規約 05 §8/§9 · M-2 · AM-25").
//   cmd: (cd frontend && nene2-check init --scan --repo nene-clear) then add stable id.
// maxLines = pinned-prettier formatted line count; maxBytes = source byte length.
// This debt ledger is shrink-monotone (REG-3): entries may only be removed, never added,
// as design.css is migrated out of @layer legacy into properly-layered CSS.
// Canonical central home is fleet-tooling registries/fleet.jsonc (REG-2 / G-7) — this
// repo-local copy is the pilot artifact; central registration is the follow-up fleet PR.
{
  "schema": "nene2-registries/1",
  "entries": [
    {
      "kind": "legacy-manifest",
      "id": "nene-clear-src-styles-design-css",
      "repo": "nene-clear",
      "path": "src/styles/design.css",
      "maxLines": 2945,
      "maxBytes": 52926
    }
  ]
}
